<UserControl x:Class="HistoryForSpotify.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HistoryForSpotify.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
                d:DataContext="{Binding DesignMenuViewModel, Source={StaticResource ViewModelLocator}}"
                DataContext="{Binding MenuViewModel, Source={StaticResource ViewModelLocator}}"
             Background="{DynamicResource MaterialDesignCardBackground}">
    <Grid>
            
    </Grid>
</UserControl>
